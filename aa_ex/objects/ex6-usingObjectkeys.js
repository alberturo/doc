/* Given the object below, use Object.keys() to iterate through the object and
print all its values.*/

const obj = {
  first: "1",
  second: 2,
  third: "three",
};

const mappedObj = Object.keys(obj);
console.log(mappedObj);
